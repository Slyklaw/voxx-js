---
phase: 01-foundation
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/engine.js
  - src/core/engine.test.js
autonomous: true
requirements:
  - TECH-03
gap_closure: true

must_haves:
  truths:
    - "Engine constructor handles async initSystems rejection gracefully"
    - "No unhandled promise warnings in console when module loading fails"
  artifacts:
    - path: src/core/engine.js
      provides: "Error handling for async system initialization"
      contains: ".catch() handler on initSystems call"
  key_links:
    - from: src/core/engine.js
      to: logger.error
      via: ".catch() handler logs initialization failures"
      pattern: "this\\.initSystems\\(\\)\\.catch"
---

<objective>
Fix the missing catch handler on the async initSystems call in Engine constructor. The current code calls this.initSystems() at line 22 without awaiting or attaching a catch handler, causing unhandled promise rejections if any module import fails.
</objective>

<context>
@.planning/phases/01-foundation/01-VERIFICATION.md
@src/core/engine.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add catch handler to initSystems promise</name>
  <files>src/core/engine.js</files>
  <action>
    At line 22, change `this.initSystems();` to `this.initSystems().catch(err => logger.error('Failed to initialize systems:', err));`

    This ensures the promise rejection is handled even though we cannot await in the constructor (constructors cannot be async). The initSystems method already throws EngineError on failure, so the catch handler just needs to log it.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=engine.test.js 2>&1 | grep -v "PASS\|FAIL" | head -20</automated>
    <manual>Check that engine.js line 22 now has .catch() handler</manual>
  </verify>
  <done>Engine constructor has .catch() handler on initSystems call, errors are logged instead of causing unhandled rejections</done>
</task>

<task type="auto">
  <name>Task 2: Add test for initSystems error handling</name>
  <files>src/core/engine.test.js</files>
  <action>
    Add a test case to verify that when initSystems fails, the error is caught and logged rather than causing an unhandled rejection.

    Mock the dynamic imports to reject, then verify:
    1. The Engine constructor does not throw
    2. The logger.error is called with the initialization failure message

    Use jest.spyOn on the logger module to verify the error logging.
  </action>
  <verify>
    <automated>npx jest src/core/engine.test.js --verbose 2>&1 | grep -E "(PASS|FAIL|✓|✗|initSystems error)"</automated>
  </verify>
  <done>Test exists that verifies initSystems errors are caught and logged, not thrown as unhandled rejections</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full test suite to ensure no regressions:
- `npm test` passes all tests
- No unhandled promise rejection warnings appear during test execution
</verification>

<success_criteria>
- engine.js line 22 has `.catch()` handler attached to initSystems call
- engine.test.js has test verifying error handling behavior
- All existing tests still pass
- No console warnings about unhandled promise rejections
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-08-SUMMARY.md`
</output>
