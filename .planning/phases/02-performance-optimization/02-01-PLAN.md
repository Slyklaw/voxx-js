---
phase: 02-performance-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - voxx-js/src/gl/render.js
  - voxx-js/src/gl/frustum.js
  - voxx-js/src/gl/performance.js
autonomous: true
requirements:
  - PERF-01

must_haves:
  truths:
    - "User sees higher FPS due to not rendering off-screen chunks"
    - "User sees fewer draw calls in debug mode (chunks culled + sorted)"
    - "Chunks outside view frustum are not rendered"
    - "Chunks with similar state are batched together"
  artifacts:
    - path: "voxx-js/src/gl/frustum.js"
      provides: "Frustum culling for chunk visibility"
      contains: "class Frustum"
    - path: "voxx-js/src/gl/render.js"
      provides: "Optimized renderChunks with culling and sorting"
      contains: "renderChunks.*sort"
    - path: "voxx-js/src/gl/performance.js"
      provides: "Draw call counter for debug mode"
      contains: "drawCalls"
  key_links:
    - from: "voxx-js/src/gl/render.js"
      to: "voxx-js/src/gl/frustum.js"
      via: "import and use Frustum class"
      pattern: "Frustum.*isVisible"
    - from: "voxx-js/src/main.js"
      to: "voxx-js/src/gl/render.js"
      via: "renderChunks call includes draw call count"
      pattern: "drawCalls.*console|debug.*drawCalls"
---

<objective>
Optimize the rendering pipeline to reduce draw calls and improve frame rates through frustum culling and chunk sorting.
</objective>

<execution_context>
@/home/box/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@voxx-js/src/gl/render.js
@voxx-js/src/gl/performance.js
@voxx-js/src/main.js
@.planning/phases/02-performance-optimization/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement frustum culling system</name>
  <files>voxx-js/src/gl/frustum.js</files>
  <action>
    Create a new Frustum class that extracts view frustum planes from the view-projection matrix.
    
    Implementation requirements:
    - Extract 6 frustum planes (near, far, left, right, top, bottom) from combined view-projection matrix
    - Implement `isBoxVisible(minX, minY, minZ, maxX, maxY, maxZ)` method using AABB-plane intersection
    - Implement `isChunkVisible(chunkX, chunkZ)` method that uses chunk world coordinates (chunkX * CHUNK_WIDTH, 0, chunkZ * CHUNK_DEPTH) to (chunkX * CHUNK_WIDTH + CHUNK_WIDTH, CHUNK_HEIGHT, chunkZ * CHUNK_DEPTH + CHUNK_DEPTH)
    - Export as named export `Frustum`
    
    Import CHUNK_WIDTH, CHUNK_HEIGHT, CHUNK_DEPTH from chunkCore.js.
  </action>
  <verify>grep -l "class Frustum" voxx-js/src/gl/frustum.js && grep -l "isChunkVisible" voxx-js/src/gl/frustum.js</verify>
  <done>Frustum class exists with isChunkVisible method that correctly culls off-screen chunks</done>
</task>

<task type="auto">
  <name>Task 2: Add draw call counter to performance metrics</name>
  <files>voxx-js/src/gl/performance.js</files>
  <action>
    Extend performance.js to track draw calls per frame:
    
    - Add `drawCalls` counter initialized to 0
    - Add `beginDrawCalls()` function to reset counter
    - Add `incrementDrawCalls(n=1)` function to increment by n
    - Add `getDrawCalls()` function to get current count
    - Modify `getMetrics()` to include drawCalls in returned object
    - Modify `logPerformance()` to include draw call count when DEBUG is true
  </action>
  <verify>grep -l "drawCalls" voxx-js/src/gl/performance.js && grep "incrementDrawCalls\|getDrawCalls" voxx-js/src/gl/performance.js</verify>
  <done>Draw call counter tracks and reports draw calls per frame</done>
</task>

<task type="auto">
  <name>Task 3: Optimize renderChunks with culling and sorting</name>
  <files>voxx-js/src/gl/render.js</files>
  <action>
    Modify the render pipeline in render.js to use frustum culling and optimize chunk rendering:
    
    1. Import Frustum class from frustum.js
    2. Import { beginDrawCalls, incrementDrawCalls } from performance.js
    3. In renderChunks function:
       - Create Frustum instance from view-projection matrix
       - Filter chunks to only include those where Frustum.isChunkVisible(chunkX, chunkZ) returns true
       - Sort visible chunks by a stable criterion (chunk key alphabetically is sufficient) for more deterministic rendering
       - Call beginDrawCalls() at start and incrementDrawCalls(1) per chunk rendered
    4. The chunk object has .chunkX and .chunkZ properties (computed from key pattern "${chunkX},${chunkZ}")
    
    Note: You need to pass chunk positions to renderChunks. Currently it ignores chunkPositions parameter. Modify to extract positions from chunks array.
  </action>
  <verify>grep -l "Frustum" voxx-js/src/gl/render.js && grep -l "beginDrawCalls\|incrementDrawCalls" voxx-js/src/gl/render.js</verify>
  <done>Rendered chunks are culled to visible frustum and draw calls are counted</done>
</task>

</tasks>

<verification>
1. Enable DEBUG mode in config.js
2. Open browser and check console for draw call count
3. Verify chunks outside view are not being rendered by looking away and checking draw call count drops
4. FPS should be more stable with frustum culling reducing wasted draw calls
</verification>

<success_criteria>
- Frustum culling prevents rendering of chunks outside view frustum
- Draw call counter accurately reflects chunks being rendered
- FPS improvement measurable through browser performance tools
</success_criteria>

<output>
After completion, create `.planning/phases/02-performance-optimization/02-01-SUMMARY.md`
</output>
