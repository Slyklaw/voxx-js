---
phase: 06-code-structure
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - voxx-js/src/main.js
  - voxx-js/src/input/InputHandler.js
  - voxx-js/src/input/index.js
  - voxx-js/src/blockEditor/BlockEditor.js
  - voxx-js/src/blockEditor/index.js
  - voxx-js/src/camera/Camera.js
  - voxx-js/src/camera/index.js
autonomous: true
requirements:
  - STRUCT-02
  - STRUCT-03

must_haves:
  truths:
    - "InputHandler class handles all keyboard/mouse input"
    - "BlockEditor class handles placeBlock/destroyBlock with shared mesh update logic"
    - "Camera class handles view matrix calculations and movement"
    - "main.js imports from new modules"
    - "All modules import correctly and game runs without errors"
  artifacts:
    - path: "voxx-js/src/input/InputHandler.js"
      provides: "Keyboard and mouse input handling"
      exports: ["InputHandler"]
    - path: "voxx-js/src/blockEditor/BlockEditor.js"
      provides: "Block placement and destruction with mesh updates"
      exports: ["BlockEditor"]
    - path: "voxx-js/src/camera/Camera.js"
      provides: "Camera position, rotation, and view matrix"
      exports: ["Camera"]
  key_links:
    - from: "voxx-js/src/main.js"
      to: "src/input/InputHandler.js"
      via: "new InputHandler()"
      pattern: "new InputHandler"
    - from: "voxx-js/src/main.js"
      to: "src/blockEditor/BlockEditor.js"
      via: "new BlockEditor()"
      pattern: "new BlockEditor"
    - from: "voxx-js/src/main.js"
      to: "src/camera/Camera.js"
      via: "new Camera()"
      pattern: "new Camera"
---

<objective>
Extract InputHandler, BlockEditor, and Camera classes from main.js into separate modules. This completes the module split for STRUCT-02 and STRUCT-03.
</objective>

<execution_context>
@/home/box/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/box/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-code-structure/06-CONTEXT.md
@.planning/phases/06-code-structure/06-01-SUMMARY.md

# Reference: main.js functions to extract
- setupControls() (lines 201-377) -> InputHandler
- destroyBlock() (lines 695-722) + placeBlock() (lines 726-772) + markNeighborChunksForUpdate() (lines 775-804) -> BlockEditor
- updateMovement() (lines 385-442) + createViewMatrix() (lines 444-464) + camera state -> Camera
- raycastBlock() (lines 502-579) -> BlockEditor (for targeting)
- updateTargetedBlock() (lines 663-691) -> BlockEditor
</context>

<tasks>

<task type="auto">
  <name>Extract InputHandler class</name>
  <files>voxx-js/src/input/InputHandler.js, voxx-js/src/input/index.js</files>
  <action>
Create src/input/InputHandler.js with the following extracted from main.js:

1. State:
   - isPointerLocked
   - keys (object to track pressed keys)
   - selectedBlockType (1-9)
   
2. Methods:
   - constructor(canvas, onBlockSelect) - setup event listeners
   - setupControls() - all event handlers from lines 201-377
   - getKeys() - return keys object for movement
   - getSelectedBlockType() - return current block selection
   - isLocked() - return pointer lock state

3. Event handlers to move:
   - pointer lock (click, pointerlockchange)
   - mouse move (camera rotation)
   - keyboard (keydown, keyup for WASD, block selection 1-9)
   - mouse wheel (block selection)
   - mousedown (block break/place - calls BlockEditor)
   - UI button handlers

4. Create src/input/index.js that exports InputHandler.
  </action>
  <verify>File exists with correct class. Test: new InputHandler() can be instantiated. Manual game test to verify controls work.</verify>
  <done>InputHandler class handles all keyboard/mouse input</done>
</task>

<task type="auto">
  <name>Extract BlockEditor class</name>
  <files>voxx-js/src/blockEditor/BlockEditor.js, voxx-js/src/blockEditor/index.js</files>
  <action>
Create src/blockEditor/BlockEditor.js with the following extracted from main.js:

1. Constructor parameters: world (World instance), gl, chunkMeshes Map
   
2. Methods:
   - destroyBlock(targetedBlock) - remove block, regenerate mesh, handle neighbors (lines 695-722)
   - placeBlock(targetedBlock, blockType) - place block, regenerate mesh, handle neighbors (lines 726-772)
   - updateTargetedBlock(raycastResult) - update targeted block state
   - markNeighborChunksForUpdate(chunkX, chunkZ, localX, localY, localZ) - handle boundary chunks (lines 775-804)
   - syncChunkToWebGL(chunk) - recreate WebGL mesh after edit (lines 806-843)

3. Shared mesh update logic (common to both placeBlock and destroyBlock):
   - chunk.setVoxel()
   - chunk.meshData = chunk.generateMeshData()
   - Delete old WebGL mesh buffers
   - Call markNeighborChunksForUpdate()

4. Create src/blockEditor/index.js that exports BlockEditor.

Note: This is the key requirement - BlockEditor handles the shared mesh update logic for both placeBlock and destroyBlock.
  </action>
  <verify>File exists with correct class. Test block placement and destruction in game.</verify>
  <done>BlockEditor class handles shared mesh update logic for placeBlock/destroyBlock</done>
</task>

<task type="auto">
  <name>Extract Camera class</name>
  <files>voxx-js/src/camera/Camera.js, voxx-js/src/camera/index.js</files>
  <action>
Create src/camera/Camera.js with the following extracted from main.js:

1. State:
   - position: { x, y, z } (default 50, 200, 50)
   - rotation: { x, y } (default 0.5, 0) - pitch and yaw
   
2. Methods:
   - constructor(initialPosition, initialRotation)
   - updateMovement(deltaTime, keys, config) - WASD + space/shift movement (lines 385-442)
   - createViewMatrix() - view matrix using lookAt (lines 444-464)
   - getPosition() / getRotation() - getters
   - setPosition() / setRotation() - setters
   - getCardinalDirection() - N/NE/E/SE/S/SW/W/NW (lines 24-36)
   
3. Helper math functions to include:
   - lookAt() - moved from utils
   - normalize(), cross(), dot() - if not using math/utils.js

4. Create src/camera/index.js that exports Camera.

Note: Camera uses math from src/math/utils.js when available.
  </action>
  <verify>File exists with correct class. Test camera movement in game.</verify>
  <done>Camera class handles view matrix calculations and movement</done>
</task>

<task type="auto">
  <name>Update main.js to use new modules</name>
  <files>voxx-js/src/main.js</files>
  <action>
Update main.js to:
1. Import InputHandler, BlockEditor, Camera from new modules
2. Replace global state and functions with class instances
3. Wire up InputHandler to call BlockEditor methods for block editing
4. Wire up main.js render loop to use Camera for view matrices
5. Remove extracted functions that are now in modules

This is the final integration - main.js becomes the orchestration layer.
  </action>
  <verify>Game runs without module import errors. All functionality works (movement, block edit, rendering).</verify>
  <done>main.js imports from new modules and game runs correctly</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Verify all module files exist with correct exports
2. Verify main.js uses new classes
3. Manual game test: run index.html in browser, verify:
   - WASD movement works
   - Camera rotation works
   - Left click destroys blocks
   - Right click places blocks
   - Block selection (1-9, scroll wheel) works
   - World renders correctly
</verification>

<success_criteria>
STRUCT-02: main.js (997 lines) is split into separate modules: InputHandler, BlockEditor, Camera, Renderer
STRUCT-03: BlockEditor class handles shared mesh update logic for placeBlock/destroyBlock
All modules import correctly and voxel engine runs without module errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-code-structure/06-02-SUMMARY.md`
</output>