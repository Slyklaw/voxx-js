---
phase: 02-rendering
plan: 05
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/core/renderer.test.js
  - src/core/renderer.js
autonomous: true
requirements:
  - TEST-05
must_haves:
  truths:
    - "Tests exist for matrix math utilities in renderer"
    - "createProjectionMatrix produces correct perspective matrix"
    - "Matrix multiplication and transformation functions work correctly"
    - "All tests pass with 'npm test'"
  artifacts:
    - path: "src/core/renderer.test.js"
      provides: "Unit tests for renderer matrix math"
      contains: "describe('Renderer matrix math', ...)"
  key_links:
    - from: "src/core/renderer.test.js"
      to: "src/core/renderer.js"
      via: "import { Renderer } from './renderer.js'"
      pattern: "import.*Renderer.*from"
    - from: "package.json"
      to: "npm test"
      via: "jest test runner"
      pattern: "\"test\":.*jest"
---

<objective>
Add unit tests for renderer matrix math calculations using TDD approach.

Purpose: Matrix math is error-prone and bugs are hard to spot visually. Unit tests verify projection matrix properties (correct aspect ratio, correct near/far clipping) and future matrix utilities (view matrix, frustum extraction).

Output: renderer.test.js with passing tests for createProjectionMatrix and matrix utility functions.
</objective>

<execution_context>
@/home/box/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/box/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/core/renderer.js
@jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create projection matrix unit tests</name>
<files>src/core/renderer.test.js</file>
  <action>
    Create TDD test file for renderer matrix math:

    1. Import at top: import { Renderer } from './renderer.js'
    2. Create WebGL mock (reuse pattern from engine.test.js - use Proxy)
    3. Create canvas mock with width=800, height=600
    4. Instantiate Renderer with mocked gl and canvas

    Test cases for createProjectionMatrix():
    - Returns Float32Array of length 16
    - Matrix is column-major (standard GL format)
    - Aspect ratio affects [0] element (f/aspect)
    - Fovy affects [5] element (f)
    - Near/far affect [10] and [14] elements
    - Matrix transforms z=0 point correctly
    - z=-near maps to w=1 (normalized device coordinate)
    - z=-far maps to w=1 (normalized device coordinate)

    Add simple matrix helper functions if needed for test assertions.
  </action>
<verify>npm test -- src/core/renderer.test.js 2>&1 | head -20</verify>
  <done>renderer.test.js created with 6+ tests for createProjectionMatrix, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add matrix utility tests and extraction tests</name>
<files>src/core/renderer.test.js</file>
  <action>
    Add tests for future matrix utilities and frustum extraction:

    1. Add tests for matrix multiplication (if added to renderer):
       - multiply(A, B) returns A * B
       - multiply(I, A) returns A (identity property)
       - multiply(A, I) returns A (identity property)

    2. Add tests for createFrustumFromMatrix (when implemented in plan 02-02):
       - Returns array of 6 planes
       - Each plane has {a, b, c, d} properties
       - Planes are normalized (length of abc = 1)

    3. Add helper function mat4Identity() for test setup
    4. Add helper function mat4Perspective(fov, aspect, near, far) for comparison

    Run all tests to verify everything passes.
  </action>
<verify>npm test -- src/core/renderer.test.js 2>&1 | tail -10</verify>
  <done>Additional matrix utility tests added, all tests pass</done>
</task>

</tasks>

<verification>
- npm test runs all renderer.test.js tests
- All tests pass
- createProjectionMatrix returns valid 4x4 matrix with correct properties
- Matrix multiplication (if tested) satisfies identity properties
- Test coverage for renderer matrix math is established
</verification>

<success_criteria>
- [ ] src/core/renderer.test.js created
- [ ] 6+ tests for createProjectionMatrix properties
- [ ] Matrix dimension and format tests pass
- [ ] Projection matrix numerical correctness verified
- [ ] Helper functions for test assertions included
- [ ] All tests pass with npm test
- [ ] TEST-05 requirement fulfilled
</success_criteria>

<output>
After completion, create `.planning/phases/02-rendering/02-05-SUMMARY.md`
</output>
