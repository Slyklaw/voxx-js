---
phase: 10-texture-rendering-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - voxx-js/shaders.js
  - voxx-js/chunk.js
  - voxx-js/blocks.js
autonomous: true
requirements:
  - TEX-04
  - TEX-05
  - TEX-06
  - TEX-07
  - TEX-08
  - TEX-09
  - TEX-10
  - TEX-11
user_setup: []

must_haves:
  truths:
    - "All blocks display textures from atlas instead of flat vertex colors"
    - "Each block type shows its designated texture from atlasPos in blocks.js"
    - "Grass blocks show green top, dirt+grass sides, and dirt bottom"
    - "UV coordinates tile correctly across block faces"
    - "Texture orientation is correct (not mirrored or rotated)"
    - "Greedy-meshed quads display textures consistently"
    - "All 5 solid block types render with correct textures"
    - "Air blocks remain transparent"
  artifacts:
    - path: "voxx-js/shaders.js"
      provides: "Fragment shader with texture sampling"
    - path: "voxx-js/chunk.js"
      provides: "UV coordinate generation for mesh"
    - path: "voxx-js/blocks.js"
      provides: "Block atlas position definitions"
  key_links:
    - from: "voxx-js/chunk.js"
      to: "voxx-js/shaders.js"
      via: "UV attributes passed to vertex shader"
    - from: "voxx-js/shaders.js"
      to: "voxx-js/blocks.js"
      via: "blockAtlasPos arrays used in fragment shader"

---

<objective>
Verify that the texture rendering pipeline works correctly — blocks display proper textures from the atlas with correct UV mapping. Add debug logging to trace block type to texture mapping through the pipeline.
</objective>

<execution_context>
@/home/box/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/box/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@voxx-js/shaders.js
@voxx-js/chunk.js
@voxx-js/blocks.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add debug logging to fragment shader for texture coordinate verification</name>
  <files>voxx-js/shaders.js</files>
  <action>
    Add debug logging capability to the fragment shader to verify texture coordinates are calculated correctly:
    1. In the CHUNK_FRAGMENT_SHADER, add a debug uniform `uniform bool debugTexture;`
    2. When debugTexture is true and blockType > 0.5, output the blockType as a color tint (e.g., multiply texColor by a debug color based on block index)
    3. This allows visual verification that different block types receive different textures
    4. Keep the default behavior (debugTexture = false) as production mode
  </action>
  <verify>
    - Shader compiles without errors
    - Debug uniform exists in fragment shader
    - When debugTexture = true, blocks show tinted textures
  </verify>
  <done>
    Fragment shader has debug texture mode for visual verification.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add UV coordinate logging in chunk mesh generation</name>
  <files>voxx-js/chunk.js</files>
  <action>
    Add logging to verify UV coordinates are generated correctly:
    1. In the mesh generation code where UVs are calculated, add a one-time log flag
    2. Log the first few UV coordinate sets generated with block type
    3. Log the UV coordinate range (min/max) to verify tiling is working
    4. Use a counter to limit logging to first 10 faces only
  </action>
  <verify>
    - Console shows UV coordinates for first faces
    - UV values are in expected 0-1 range (or tiled multiples)
    - Block types are correctly associated with UV coordinates
  </verify>
  <done>
    Chunk mesh generation logs UV coordinates for verification.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify block atlas positions in blocks.js</name>
  <files>voxx-js/blocks.js</files>
  <action>
    Add verification logging for block atlas positions:
    1. In getBlockAtlasPositions(), add a log that dumps all block type atlas positions
    2. Log should show: Block type name, top position, sides position, bottom position
    3. This verifies that block definitions match the texture atlas layout
    4. Add the log at module initialization time (outside function, or called once)
  </action>
  <verify>
    - Console shows all block types with their atlas positions
    - Positions are non-zero for solid blocks
    - Grass shows different positions for top vs sides vs bottom
  </verify>
  <done>
    Block atlas positions logged at startup for verification.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add texture rendering success indicator</name>
  <files>voxx-js/renderer.js</files>
  <action>
    Add a final verification log when chunks are first rendered with textures:
    1. In updateChunkMeshes, when a mesh with textureAtlas uniform is added, log once
    2. Log: "First chunk rendered with texture atlas" and count of chunk meshes
    3. This confirms the full pipeline from load → uniform → render is working
    4. Use a class-level flag to ensure one-time logging
  </action>
  <verify>
    - Console shows "First chunk rendered with texture atlas" when game starts
    - Log shows chunk count
  </verify>
  <done>
    Renderer confirms texture pipeline is active on first chunk render.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Load the game in browser
2. Open developer console
3. Verify the following sequence appears:
   - [Texture] blockAtlasPositions log showing all block types and their atlas coordinates
   - [Texture] UV coordinates logged for first few faces
   - [Texture] First chunk rendered with texture atlas
4. Visually verify blocks show textures (not flat colors)
5. Verify grass blocks show different textures on top vs sides
6. Verify no errors in console about shader compilation or texture sampling
</verification>

<success_criteria>
- [ ] Block atlas positions logged and verified correct
- [ ] UV coordinates generated and logged for mesh faces
- [ ] Fragment shader has debug mode for texture verification
- [ ] Renderer confirms texture pipeline active on first chunk render
- [ ] Console logs provide traceability through entire texture pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/10-texture-rendering-validation/10-01-SUMMARY.md`
</output>
