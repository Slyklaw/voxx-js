---
phase: 01-ui-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [voxx-js/src/main.js]
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "User can see compass needle pointing to cardinal direction (N/S/E/W) as they rotate camera"
    - "User can see player facing direction indicator (e.g., 'NW', 'SE') updating in real-time"
    - "User can see clock display showing day/night cycle progress (e.g., 'Dawn', 'Day', 'Dusk', 'Night')"
    - "User can observe visual indicator of time progression"
  artifacts:
    - path: "voxx-js/src/main.js"
      provides: "Compass and clock UI update logic in render loop"
      min_lines: 40
      contains: ["getCardinalDirection", "getDayPhase", "compassNeedle", "compass-text"]
  key_links:
    - from: "main.js render function"
      to: ".compass-text"
      via: "getElementByClass + textContent update"
      pattern: "querySelector.*compass-text.*textContent"
    - from: "main.js render function"
      to: ".compass-needle"
      via: "getElementByClass + transform update"
      pattern: "querySelector.*compass-needle.*style.transform"
    - from: "main.js render function"
      to: "#time-display"
      via: "getElementById + textContent update with phase"
      pattern: "getElementById.*time-display"
    - from: "cameraRotation.y"
      to: "compass display"
      via: "yaw converted to degrees and normalized"
      pattern: "cameraRotation\\.y.*180.*PI"
---

<objective>
Implement functional compass and clock UI elements by connecting existing HTML elements to game data sources.

Purpose: Provide real-time player feedback showing facing direction and time of day
Output: Updated main.js with compass and clock update logic
</objective>

<execution_context>
@/home/box/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/box/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ui-fixes/01-CONTEXT.md
@.planning/phases/01-ui-fixes/01-RESEARCH.md

@voxx-js/src/main.js (existing time display at lines 790-791, cameraRotation at line 16)
@voxx-js/index.html (compass elements at lines 17-26)
@voxx-js/style.css (compass styling at lines 58-130)
</context>

<tasks>

<task type="auto">
  <name>Add compass update logic to render loop</name>
  <files>voxx-js/src/main.js</files>
  <action>
Add compass functionality inside the render() function, near the existing time display update (around line 791):

1. Add helper function `getCardinalDirection(degrees)` that converts yaw angle (0-360) to N/NE/E/SE/S/SW/W/NW using 45-degree segments centered on 22.5° offsets

2. Add compass text update: Find `.compass-text` element, calculate direction from `cameraRotation.y`, update textContent

3. Add compass needle rotation: Find `.compass-needle` element, set transform to rotate based on negative cameraRotation.y (inverted for compass display)

IMPORTANT: Do not create new DOM elements — only update existing ones. Use CSS transform for needle rotation. Add 0.1s CSS transition for smooth animation.
  </action>
  <verify>Manual verification: Rotate camera with mouse and verify compass direction text and needle update in real-time</verify>
  <done>Compass shows N/S/E/W direction text and needle rotates matching player facing direction</done>
</task>

<task type="auto">
  <name>Enhance clock with day/night cycle phases</name>
  <files>voxx-js/src/main.js</files>
  <action>
Enhance the existing time display at line 791 to include day phase indicators:

1. Add helper function `getDayPhase(hours)` returning 'Dawn' (5-7), 'Day' (7-17), 'Dusk' (17-19), 'Night' (19-5)

2. Modify existing time display update at line 791 to include phase: format becomes "Day: HH:MM (Dawn)" or "Night: 22:30 (Night)"

IMPORTANT: Keep the existing "Day/Night: HH:MM" format and ADD phase in parentheses. Do not replace existing functionality.
  </action>
  <verify>Manual verification: Wait for time to progress or observe different times showing Dawn/Day/Dusk/Night phases</verify>
  <done>Clock displays show time-of-day phases (Dawn/Day/Dusk/Night) alongside existing time</done>
</task>

</tasks>

<verification>
- Compass direction text updates when camera rotates (N, NE, E, SE, S, SW, W, NW)
- Compass needle rotates matching player facing direction
- Time display shows "Day/Night: HH:MM (Phase)" format
- All updates happen in real-time during render loop
</verification>

<success_criteria>
Phase 1 complete when:
- [ ] Compass displays cardinal direction text (UI-01)
- [ ] Compass needle rotates correctly (UI-01)
- [ ] Clock shows day/night cycle phases (UI-02)
- [ ] Both UI elements update in real-time as player moves/time passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-fixes/01-01-SUMMARY.md`
</output>
