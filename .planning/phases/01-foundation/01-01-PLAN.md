---
phase: 01-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/core/constants.js
  - package.json
  - jest.config.js
  - src/core/constants.test.js
autonomous: true
requirements:
  - TECH-02
  - TECH-07
  - TEST-01

must_haves:
  truths:
    - "src/core/constants.js exports all game constants with UPPER_SNAKE_CASE names"
    - "package.json exists with Jest configured as dev dependency"
    - "npm test runs Jest successfully"
    - "No hardcoded chunkSize=32 values remain in source files"
  artifacts:
    - path: "src/core/constants.js"
      provides: "Shared constants module"
      exports: "CHUNK_SIZE, WORLD_SEED, VIEW_DISTANCE, GRAVITY, etc."
    - path: "package.json"
      provides: "Project configuration with Jest"
      contains: "jest.config, scripts.test"
    - path: "jest.config.js"
      provides: "Jest configuration for ES modules"
    - path: "src/core/constants.test.js"
      provides: "Constants validation tests"
  key_links:
    - from: "src/chunks/chunk-manager.js"
      to: "src/core/constants.js"
      via: "import { CHUNK_SIZE } from '../core/constants.js'"
    - from: "src/core/world.js"
      to: "src/core/constants.js"
      via: "import { CHUNK_SIZE } from './constants.js'"
---

<objective>
Create shared constants module and initialize project with Jest test framework
</objective>

Purpose: Eliminate hardcoded magic numbers and establish testing infrastructure before other foundation work
Output: src/core/constants.js with all game constants, package.json with Jest, initial test file

<execution_context>
@/home/box/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@src/core/world.js
@src/chunks/chunk-manager.js
@src/chunks/chunk.js
@src/player/player.js
</context>

<tasks>

<task type="tdd" name="Task 1: Create constants module with TDD">
  <files>src/core/constants.js, src/core/constants.test.js</files>
  <action>
Write tests FIRST for the constants module, then implement.

**RED phase:** Create src/core/constants.test.js with tests:
- CHUNK_SIZE equals 32
- VIEW_DISTANCE equals 8
- All constants use UPPER_SNAKE_CASE naming
- Constants are immutable (cannot be reassigned)
- All expected constants exist (CHUNK_SIZE, VIEW_DISTANCE, WORLD_SEED_DEFAULT, GRAVITY, JUMP_STRENGTH, WALK_SPEED, MOUSE_SENSITIVITY)

**GREEN phase:** Create src/core/constants.js implementing the constants:
```javascript
export const CHUNK_SIZE = 32;
export const VIEW_DISTANCE = 8;
export const WORLD_SEED_DEFAULT = 42;
export const GRAVITY = 20.0;
export const JUMP_STRENGTH = 8.0;
export const WALK_SPEED = 5.0;
export const MOUSE_SENSITIVITY = 0.002;
```

Run tests to confirm they pass.
  </action>
  <verify>npm test -- --testPathPattern=constants.test.js</verify>
  <done>src/core/constants.js exports all required constants, tests pass</done>
</task>

<task type="auto" name="Task 2: Replace hardcoded values in source files">
  <files>src/core/world.js, src/chunks/chunk-manager.js, src/chunks/chunk.js, src/player/player.js</files>
  <action>
Replace all hardcoded magic numbers with imports from src/core/constants.js:

1. In src/core/world.js:
   - Replace `this.chunkSize = 32` with `import { CHUNK_SIZE }` and `this.chunkSize = CHUNK_SIZE`
   - Replace `Math.random() * 10000` seed with `import { WORLD_SEED_DEFAULT }` and `this.worldSeed = WORLD_SEED_DEFAULT`

2. In src/chunks/chunk-manager.js:
   - Replace `this.chunkSize = 32` with `import { CHUNK_SIZE }` and `this.chunkSize = CHUNK_SIZE`
   - Replace `this.viewDistance = 8` with `import { VIEW_DISTANCE }` and `this.viewDistance = VIEW_DISTANCE`

3. In src/chunks/chunk.js:
   - Replace hardcoded `32` in constructor data array and getVoxelIndex with `import { CHUNK_SIZE }`
   - Update bounds checking to use CHUNK_SIZE constant

4. In src/player/player.js:
   - Replace `this.walkSpeed = 5.0` with `import { WALK_SPEED }`
   - Replace `this.jumpStrength = 8.0` with `import { JUMP_STRENGTH }`
   - Replace `this.gravity = 20.0` with `import { GRAVITY }`
   - Replace `this.mouseSensitivity = 0.002` with `import { MOUSE_SENSITIVITY }`

Use replaceAll where appropriate for repeated constants.
  </action>
  <verify>grep -r "= 32" src/ should show no matches (excluding comments)</verify>
  <done>All hardcoded magic numbers replaced with constants imports</done>
</task>

<task type="auto" name="Task 3: Initialize package.json with Jest">
  <files>package.json, jest.config.js</files>
  <action>
1. Create package.json if not exists:
```bash
npm init -y
```

2. Install Jest:
```bash
npm install --save-dev jest @jest/globals
```

3. Create jest.config.js for ES modules support:
```javascript
export default {
  testEnvironment: 'node',
  transform: {},
  extensionsToTreatAsEsm: ['.js'],
  moduleNameMapper: {
    '^(\\.{1,2}/.*)\\.js$': '$1',
  },
};
```

4. Add test script to package.json:
```json
"scripts": {
  "test": "node --experimental-vm-modules node_modules/.bin/jest"
}
```
  </action>
  <verify>npm test -- --passWithNoTests</verify>
  <done>package.json exists with Jest configured, npm test runs successfully</done>
</task>

</tasks>

<verification>
- Constants module exports all required values
- No hardcoded magic numbers remain in source
- Jest configured and tests pass
- package.json has proper test script
</verification>

<success_criteria>
- [ ] src/core/constants.js created with all game constants
- [ ] package.json exists with Jest dependency
- [ ] jest.config.js supports ES modules
- [ ] npm test runs successfully
- [ ] All hardcoded magic numbers replaced with constant imports
- [ ] Tests validate constant values and naming conventions
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
