---
phase: 03-performance
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - voxx-js/src/chunk/chunk.js
  - voxx-js/src/world.js
autonomous: true
requirements:
  - PERF-03
must_haves:
  truths:
    - "Neighbor chunk references are cached, not recalculated per access"
    - "Dirty flags track when mesh needs rebuild due to neighbor changes"
    - "No new objects created for neighbor lookups per frame"
  artifacts:
    - path: "voxx-js/src/chunk/chunk.js"
      provides: "neighborChunks cache, neighborsDirty flag, getNeighbors()"
    - path: "voxx-js/src/world.js"
      provides: "Uses cached neighbors from chunks"
---

<objective>
Implement neighbor reference caching with dirty flags for incremental updates.

Purpose: Cache neighbor chunk references instead of recalculating on each access; track changes with dirty flags to only rebuild meshes when needed.

Output: Chunk class with cached neighbor references and dirty flag tracking.
</objective>

<execution_context>
@/home/box/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/box/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-performance/03-RESEARCH.md
@voxx-js/src/world.js
@voxx-js/src/chunk/chunk.js
</context>

<tasks>

<task type="auto">
  <name>Add neighbor cache and dirty flag to Chunk class</name>
  <files>voxx-js/src/chunk/chunk.js</files>
  <action>
    Extend Chunk class with neighbor caching:

    1. Add to constructor:
       ```javascript
       this.neighborChunks = { north: null, south: null, east: null, west: null };
       this.neighborsDirty = false;
       ```
    2. Add `setNeighbors(north, south, east, west)` method that:
       - Checks if any neighbor reference changed
       - Updates this.neighborChunks
       - Sets this.neighborsDirty = true if any neighbor changed
    3. Add `getNeighbors()` method that returns cached this.neighborChunks
    4. Add `markNeighborsClean()` to reset dirty flag after mesh rebuild
    5. Add `needsMeshRebuild()` method: returns this.needsUpdate || this.neighborsDirty

    This replaces world.js:142-149 which creates new objects each call.
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | head -30</automated>
    <manual>Check: Chunk instances have neighborChunks property, dirty flag toggles on neighbor change</manual>
  </verify>
  <done>Chunk class caches neighbor references with dirty flag tracking</done>
</task>

<task type="auto">
  <name>Update world.js to use neighbor caching</name>
  <files>voxx-js/src/world.js</files>
  <action>
    Modify world.js to use cached neighbors instead of recalculating:

    1. In `setupChunkNeighbors()` (lines 107-139), call chunk.setNeighbors() instead of creating new objects
    2. Remove `getChunkNeighbors()` that creates new object each call
    3. Update code that calls getChunkNeighbors() to use chunk.getNeighbors() instead
    4. After mesh rebuild completes, call chunk.markNeighborsClean()

    Key: Existing neighborChunks already exist in some form; add dirty flag tracking.
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | head -30</automated>
    <manual>Verify: Neighbor lookups return cached references, no new objects created per frame</manual>
  </verify>
  <done>World.js uses cached neighbor references from chunks</done>
</task>

<task type="auto">
  <name>Add incremental update on chunk unload</name>
  <files>voxx-js/src/chunk/chunk.js</files>
  <action>
    Ensure proper cleanup when chunks unload:

    1. Add `clearNeighbors()` method that sets all neighbor references to null
    2. In chunk dispose/cleanup, call clearNeighbors() to prevent stale references
    3. When a chunk unloads, mark its neighbors as dirty (they need mesh rebuild for exposed faces)

    This prevents stale pointer issues from RESEARCH.md Pitfall 2.
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | head -30</automated>
    <manual>Unload a chunk, verify neighbors are marked dirty and can rebuild mesh</manual>
  </verify>
  <done>Chunk neighbors properly cleaned up on unload</done>
</task>

</tasks>

<verification>
- [ ] Chunk has neighborChunks property with cached references
- [ ] neighborsDirty flag tracks when neighbors change
- [ ] needsMeshRebuild() returns true when neighborsDirty is true
- [ ] No new objects created for neighbor lookups per frame
- [ ] Chunk unload properly clears neighbor references
</verification>

<success_criteria>
Neighbor calculations are cached and don't repeat per chunk load (PERF-03)

Observable truth: Neighbor lookups return cached references; mesh rebuilds only when dirty flag is set
Artifact: Chunk.getNeighbors() returns cached object; setNeighbors() updates cache and sets dirty flag
</success_criteria>

<output>
After completion, create `.planning/phases/03-performance/03-03-SUMMARY.md`
</output>
