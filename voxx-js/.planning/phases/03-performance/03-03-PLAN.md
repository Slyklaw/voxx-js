---
phase: 03-performance
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - voxx-js/voxx-js/world.js
  - voxx-js/voxx-js/chunk.js
autonomous: true
requirements:
  - PERF-03
must_haves:
  truths:
    - "Neighbor references are cached on chunk object"
    - "No new object allocation per neighbor lookup"
    - "Neighbor dirty flag triggers incremental mesh rebuild"
  artifacts:
    - path: "voxx-js/voxx-js/chunk.js"
      provides: "Cached neighbor references with dirty flag"
      contains: "neighborChunks\|neighborsDirty"
    - path: "voxx-js/voxx-js/world.js"
      provides: "getChunkNeighbors returns cached references"
      contains: "cachedNeighbors"
  key_links:
    - from: "world.js"
      to: "chunk.js"
      via: "chunk.neighborChunks"
      pattern: "neighborChunks"
---

<objective>
Cache neighbor calculations and use dirty flags for incremental updates.

Purpose: Currently getChunkNeighbors() creates new object on each call and setupChunkNeighbors() recalculates on every chunk load. Caching with dirty flags prevents redundant calculations.

Output: Modified chunk.js with cached neighbor references, updated world.js to use cached lookups
</objective>

<context>
@voxx-js/voxx-js/world.js
@voxx-js/voxx-js/chunk.js
@.planning/phases/03-performance/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Add neighbor caching to chunk.js</name>
  <files>voxx-js/voxx-js/chunk.js</files>
  <action>
1. Add neighborChunks property initialized to { north: null, south: null, east: null, west: null }
2. Add neighborsDirty flag initialized to false
3. Modify setNeighbors() to:
   a. Compare new neighbors with existing references
   b. Set neighborsDirty = true only when neighbor actually changes
   c. Always update neighborChunks reference
4. Add needsMeshRebuild() method that returns: this.needsUpdate || this.neighborsDirty
5. Clear neighborsDirty after mesh rebuild completes
  </action>
  <verify>
grep -n "neighborChunks\|neighborsDirty" voxx-js/voxx-js/chunk.js
  </verify>
  <done>Chunk stores neighbor references with dirty flag tracking</done>
</task>

<task type="auto">
  <name>Update world.js to use cached neighbor lookups</name>
  <files>voxx-js/voxx-js/world.js</files>
  <action>
1. Modify getChunkNeighbors() to return cached chunk.neighborChunks instead of creating new object
2. Add cache invalidation when chunk is unloaded - set all references to null
3. In setupChunkNeighbors(), call chunk.setNeighbors() directly instead of building new object
4. After mesh rebuild completes, set chunk.neighborsDirty = false
  </action>
  <verify>
grep -n "getChunkNeighbors\|neighborChunks" voxx-js/voxx-js/world.js
  </verify>
  <done>getChunkNeighbors returns cached references, no new objects created per call</done>
</task>

<task type="auto">
  <name>Verify neighbor caching works correctly</name>
  <files>voxx-js/voxx-js/chunk.js</files>
  <action>
1. Load the application
2. Move around to trigger chunk loads/unloads
3. Verify no null reference errors in console when chunks load
4. Verify mesh rebuilds occur only when neighbors actually change (not on every chunk load)
  </action>
  <verify>
Manual: Open browser, move to load new chunks, check console for errors
  </verify>
  <done>Neighbor caching works without errors, mesh rebuilds are incremental</done>
</task>

</tasks>

<verification>
PERF-03 criteria verified when:
- chunk.js has neighborChunks and neighborsDirty properties
- world.js getChunkNeighbors returns cached references
- No new object allocation in getChunkNeighbors
</verification>

<success_criteria>
Neighbor calculations are cached and don't repeat per chunk load - verified by checking chunk.neighborChunks is populated and reused, not recreated
</success_criteria>

<output>
After completion, create .planning/phases/03-performance/03-03-SUMMARY.md
</output>
