---
phase: 05-polish-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - voxx-js/src/gl/performance.js
  - voxx-js/src/gl/render.js
  - voxx-js/src/chunk/chunkManager.js
autonomous: true
requirements:
  - PERF-01
  - PERF-02
user_setup: []

must_haves:
  truths:
    - "Application maintains 60fps with render distance of 8+ chunks"
    - "Only modified chunks rebuild meshes; unchanged chunks retain buffers"
    - "No visible seams between chunks (boundary gaps resolved)"
    - "Resource disposal verified, no memory growth over 10+ minutes of play"
  artifacts:
    - path: "voxx-js/src/gl/performance.js"
      provides: "FPS monitoring and performance tracking"
      min_lines: 50
    - path: "voxx-js/src/gl/render.js"
      provides: "Updated render loop with FPS tracking"
      exports: ["renderLoop", "getFPS"]
    - path: "voxx-js/src/chunk/chunkManager.js"
      provides: "Chunk management with dirty tracking"
      exports: ["getChunk", "markDirty", "getRenderDistance"]
  key_links:
    - from: "voxx-js/src/gl/render.js"
      to: "voxx-js/src/gl/performance.js"
      via: "FPS calculation in render loop"
      pattern: "getFPS.*deltaTime"
    - from: "voxx-js/src/chunk/chunkManager.js"
      to: "voxx-js/src/gl/buffers.js"
      via: "Buffer rebuild only on dirty chunks"
      pattern: "markDirty.*rebuild"
---

<objective>
Implement performance optimization system for voxel rendering pipeline.

Purpose: Ensure application maintains 60fps with 8+ chunk render distance, efficient buffer updates, no memory leaks.
Output: Performance monitoring module, optimized chunk update system, memory verification.
</objective>

<execution_context>
@/home/box/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/box/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-rendering-pipeline/02-01-SUMMARY.md
@.planning/phases/02-core-rendering-pipeline/02-02-SUMMARY.md
@voxx-js/src/gl/render.js
@voxx-js/src/gl/buffers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FPS monitoring system</name>
  <files>voxx-js/src/gl/performance.js</files>
  <action>
Create performance.js module with:
- FPS counter using frame time tracking (track last N frames for stability)
- Frame time monitoring (min, max, average)
- Performance metrics object with getFPS(), getFrameTime(), getMetrics()
- Render loop integration helpers

Use rolling average over 60 frames for stable FPS display. Track delta time for consistent animation speed.
  </action>
  <verify>
  <automated>Node.js syntax check: node --check voxx-js/src/gl/performance.js 2>&1 || echo "ES module - check with dev server"</automated>
  <manual>Import the module and call getFPS() - should return number between 0-120</manual>
  </verify>
  <done>getFPS() returns valid frame rate, frame time tracking works</done>
</task>

<task type="auto">
  <name>Task 2: Implement chunk dirty tracking system</name>
  <files>voxx-js/src/chunk/chunkManager.js</files>
  <action>
Create chunkManager.js with:
- Chunk storage Map with position keys
- Dirty flag tracking per chunk (setDirty(chunkKey), clearDirty(chunkKey), isDirty(chunkKey))
- getChunksInRange(centerX, centerZ, renderDistance) - returns chunks within render distance
- renderDistance configuration (default 8 chunks)
- Only rebuild VAO/VBO for dirty chunks; skip clean chunks

Integrate with existing buffers.js - only call rebuildMesh() when chunk.isDirty is true.
  </action>
  <verify>
  <automated>grep -l "markDirty\|isDirty\|getRenderDistance" voxx-js/src/chunk/chunkManager.js</automated>
  <manual>Call markDirty() on a chunk, verify isDirty() returns true, after rebuild verify false</manual>
  </verify>
  <done>Dirty chunks identified, clean chunks skipped during render updates</done>
</task>

<task type="auto">
  <name>Task 3: Integrate FPS tracking into render loop</name>
  <files>voxx-js/src/gl/render.js</files>
  <action>
Update render.js to:
- Import and initialize performance module
- Calculate delta time between frames
- Call getFPS() each frame
- Log FPS to console every 60 frames (every ~1 second)
- Ensure render loop uses requestAnimationFrame with delta time

Add public API: getFPS() that exposes current FPS to external callers.
  </action>
  <verify>
  <automated>grep -l "getFPS\|performance\." voxx-js/src/gl/render.js</automated>
  <manual>Run application, verify FPS logs appear in console every ~1 second</manual>
  </done>
  <done>Render loop logs FPS, getFPS() returns current frame rate</done>
</task>

</tasks>

<verification>
1. Run application with 8+ chunk render distance
2. Verify FPS counter displays in console (target: 60fps)
3. Modify a block - only affected chunk should rebuild (check via console logs)
4. Play for 5+ minutes - verify no memory growth in DevTools Memory tab
</verification>

<success_criteria>
- FPS monitoring functional (getFPS() returns valid number)
- Render distance configurable to 8+ chunks
- Only dirty chunks rebuild meshes; clean chunks preserved
- No memory leaks detected during extended play
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-performance/05-01-SUMMARY.md`
</output>
