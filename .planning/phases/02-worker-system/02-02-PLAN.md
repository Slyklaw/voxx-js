---
phase: 02-worker-system
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - voxx-js/chunk.js
  - voxx-js/world.js
  - voxx-js/src/blockEditor/BlockEditor.js
autonomous: true
requirements:
  - WRK-02
must_haves:
  truths:
    - "Block changes always result in correct mesh updates within 1 frame"
    - "Worker mesh completion does not overwrite main-thread generated mesh"
  artifacts:
    - path: "voxx-js/chunk.js"
      provides: "Chunk mesh state machine"
      contains: "meshState"
  key_links:
    - from: "voxx-js/world.js"
      to: "voxx-js/chunk.js"
      via: "chunk.meshState check before fromWorkerMesh"
      pattern: "meshState.*idle"
    - from: "voxx-js/src/blockEditor/BlockEditor.js"
      to: "voxx-js/chunk.js"
      via: "set meshState during block edit"
      pattern: "meshState.*generating"
---

<objective>
Implement chunk mesh state machine to prevent race conditions between worker mesh generation and main thread block edits.
</objective>

<execution_context>
@/home/box/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/box/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-worker-system/02-RESEARCH.md
@voxx-js/chunk.js
@voxx-js/world.js
@voxx-js/src/blockEditor/BlockEditor.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mesh state machine to Chunk class</name>
  <files>voxx-js/chunk.js</files>
  <action>
    Add mesh state tracking to Chunk class to prevent concurrent mesh updates:

    1. Add `this.meshState = 'idle'` property in constructor (states: 'idle' | 'generating' | 'ready' | 'error')

    2. Modify `updateMesh()` to set meshState to 'generating' before generating and 'ready' after

    3. Modify `fromWorkerMesh()` to only apply mesh if meshState is 'idle':
       - If meshState !== 'idle', skip applying worker mesh (main thread is editing)
       - Add comment explaining the synchronization logic

    4. Ensure `dispose()` resets meshState to 'idle'
  </action>
  <verify>
    npm test -- --grep "chunk" 2>/dev/null || echo "No tests found - manual verification needed"
  </verify>
  <done>
    Chunk has meshState property with states: idle, generating, ready, error
  </done>
</task>

<task type="auto">
  <name>Task 2: Update World to check meshState before applying worker mesh</name>
  <files>voxx-js/world.js</files>
  <action>
    Modify World worker completion handler to respect meshState:

    1. In `getChunk()` onComplete callback, before calling `fromWorkerMesh()`:
       ```javascript
       if (chunkData.meshData && chunk.chunkState === 'idle') {
         this.chunks[key].fromWorkerMesh(chunkData.meshData);
       }
       ```
    2. If chunkState !== 'idle', the main thread is editing - worker result is discarded (main thread will regenerate)
  </action>
  <verify>
    npm test -- --grep "world" 2>/dev/null || echo "No tests found - manual verification needed"
  </verify>
  <done>
    World only applies worker mesh when chunk is idle
  </done>
</task>

<task type="auto">
  <name>Task 3: Update BlockEditor to lock meshState during edits</name>
  <files>voxx-js/src/blockEditor/BlockEditor.js</files>
  <action>
    Lock meshState during block edits to prevent worker race:

    1. In `destroyBlock()` and `placeBlock()`:
       - Before calling `chunk.setVoxel()`, set `chunk.meshState = 'generating'`
       - After calling `syncChunkToWebGL()`, set `chunk.meshState = 'ready'`

    2. In `markNeighborChunksForUpdate()`:
       - Set neighbor chunk meshState to 'generating' before regenerating

    3. This ensures worker responses arriving during block edits are ignored
  </action>
  <verify>
    npm test -- --grep "block" 2>/dev/null || echo "No tests found - manual verification needed"
  </verify>
  <done>
    Block edits lock meshState to prevent worker mesh overwrites
  </done>
</task>

</tasks>

<verification>
- [ ] Chunk has meshState with states: idle, generating, ready, error
- [ ] World checks meshState before applying worker mesh
- [ ] BlockEditor locks meshState during edits
- [ ] Block changes result in correct mesh updates within 1 frame
</verification>

<success_criteria>
Block changes always result in correct mesh updates within 1 frame (criterion 2)
</success_criteria>

<output>
After completion, create .planning/phases/02-worker-system/02-02-SUMMARY.md
</output>
