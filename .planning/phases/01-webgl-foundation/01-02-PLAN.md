---
phase: 01-webgl-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - voxx-js/src/gl/context.js
  - voxx-js/src/main.js
  - voxx-js/src/gl/render.js
autonomous: true
requirements:
  - GL-02

must_haves:
  truths:
    - "WebGL context loss is detected and handled automatically without page reload"
    - "User sees brief on-screen notification during context recovery"
  artifacts:
    - path: "voxx-js/src/gl/context.js"
      provides: "Context loss event handlers with listener notifications"
      exports: ["handleContextLost", "handleContextRestored", "addContextLossListener"]
    - path: "voxx-js/src/main.js"
      provides: "Context lost notification overlay element"
      creates: "div#context-lost-overlay"
    - path: "voxx-js/src/gl/render.js"
      provides: "Render loop that checks isContextLost() before drawing"
      checks: "isContextLost() before WebGL calls"
  key_links:
    - from: "context.js handleContextLost"
      to: "main.js overlay"
      via: "addContextLossListener callback"
    - from: "render.js render loop"
      to: "context.js isContextLost"
      via: "if (isContextLost()) return early"
---

<objective>
Implement automatic WebGL context loss detection and recovery with on-screen notification, per user decision: auto-recovery, brief "Reconnecting..." overlay, no user action required.
</objective>

<context>
@voxx-js/src/gl/context.js
@voxx-js/src/main.js
@voxx-js/src/gl/render.js
@.planning/phases/01-webgl-foundation/01-CONTEXT.md
@.planning/phases/01-webgl-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Add context lost notification overlay to main.js</name>
  <files>voxx-js/src/main.js</files>
  <action>
Add a simple HTML overlay div to the DOM in main.js that shows "Reconnecting..." message. Create functions showContextLostNotification() and hideContextLostNotification() that toggle the overlay visibility. Style with CSS to be centered, semi-transparent dark background, white text, visible during recovery.

The overlay should be hidden by default and only shown during context loss/restore events per user decision: "brief on-screen notification during recovery".
  </action>
  <verify>
grep -n "context-lost-overlay" voxx-js/src/main.js
  </verify>
  <done>
Overlay element exists and can be shown/hidden via JavaScript
</done>
</task>

<task type="auto">
  <name>Wire context loss handlers to UI and render loop</name>
  <files>voxx-js/src/gl/context.js, voxx-js/src/gl/render.js</files>
  <action>
Enhance context.js handleContextLost() to call showContextLostNotification() and handleContextRestored() to call hideContextLostNotification(). Use addContextLossListener to wire these up, or modify the handlers directly.

Modify render.js (or wherever the main render loop is) to check isContextLost() at the start of each frame and return early if context is lost. This prevents WebGL errors during context loss. Also add a check after context restore to reinitialize all resources.

Per 01-RESEARCH.md lines 193-202: The render loop must check gl.isContextLost() or use isContextLost() before making WebGL calls.
  </action>
  <verify>
grep -n "isContextLost" voxx-js/src/gl/render.js
grep -n "showContextLostNotification\|hideContextLostNotification" voxx-js/src/gl/context.js
  </verify>
  <done>
Context loss triggers UI notification, render loop skips drawing during context loss
</done>
</task>

</tasks>

<verification>
Manual test: Use browser dev tools "Emulate WebGL loss" feature (available in Chrome/Firefox). Context should be lost, overlay should appear, context should restore automatically, overlay should hide, rendering should resume.
</verification>

<success_criteria>
- Context loss detected via webglcontextlost event
- On-screen "Reconnecting..." overlay appears during context loss
- Context restored automatically without page reload
- Render loop handles context loss gracefully (no errors)
</success_criteria>

<output>
After completion, create `.planning/phases/01-webgl-foundation/01-02-SUMMARY.md`
</output>
