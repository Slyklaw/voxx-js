---
phase: 01-webgl-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - voxx-js/src/gl/performance.js
  - voxx-js/src/gl/render.js
autonomous: true
requirements:
  - GL-03

must_haves:
  truths:
    - "Rendering pipeline maintains 60fps with default settings on mid-range hardware"
    - "Console warning logged when fps drops below 50"
  artifacts:
    - path: "voxx-js/src/gl/performance.js"
      provides: "FPS tracking with warning threshold"
      exports: ["getFPS", "checkFPSWarning"]
    - path: "voxx-js/src/gl/render.js"
      provides: "Render loop that checks and logs FPS warnings"
      calls: "checkFPSWarning() each frame"
  key_links:
    - from: "render.js render loop"
      to: "performance.js checkFPSWarning"
      via: "checkFPSWarning() call in frame loop"
---

<objective>
Add FPS monitoring with console warning when frame rate drops below 50fps, per user decision: "log warning to console if fps drops below 50, let user manually adjust quality in Phase 4".
</objective>

<context>
@voxx-js/src/gl/performance.js
@voxx-js/src/gl/render.js
@voxx-js/src/gl/context.js
@.planning/phases/01-webgl-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Add FPS warning function to performance.js</name>
  <files>voxx-js/src/gl/performance.js</files>
  <action>
Check if performance.js already has FPS calculation. If not, implement getFPS() that tracks frame times using performance.now() and returns current FPS.

Add checkFPSWarning() function that:
- Calls getFPS() to get current FPS
- If fps < 50 and fps > 0 (to avoid warning during context loss or initial load), logs console.warn with message like "Low FPS detected: {X} fps - consider reducing quality settings"
- Uses a throttle/counter to avoid spamming (e.g., warn once per 5 seconds, not every frame)

Per user constraint: "No automatic quality adjustment, log warning to console if fps drops below 50"
  </action>
  <verify>
grep -n "checkFPSWarning\|getFPS" voxx-js/src/gl/performance.js
  </verify>
  <done>
performance.js exports checkFPSWarning function that logs warning when fps < 50
</done>
</task>

<task type="auto">
  <name>Integrate FPS check into render loop</name>
  <files>voxx-js/src/gl/render.js</files>
  <action>
Find the main render loop in render.js (or main.js if render.js is just a module). Add a call to checkFPSWarning() at the start or end of each frame. Ensure it's called every frame but the warning is throttled per the implementation in performance.js.

The render loop should already check isContextLost() from plan 01-02; ensure FPS check happens after context check or is skipped during context loss.
  </action>
<verify>
grep -n "checkFPSWarning" voxx-js/src/gl/render.js
  </verify>
  <done>
Render loop calls checkFPSWarning each frame
</done>
</task>

</tasks>

<verification>
Manual test: Run application, observe console. FPS warning should appear if frame rate drops below 50 (can test by adding debug frame delays or running on lower-end hardware).
</verification>

<success_criteria>
- FPS is tracked and available via getFPS()
- Console warning logged when fps drops below 50
- Warning is throttled to avoid spam
- No automatic quality adjustment (per user constraint)
</success_criteria>

<output>
After completion, create `.planning/phases/01-webgl-foundation/01-03-SUMMARY.md`
</output>
