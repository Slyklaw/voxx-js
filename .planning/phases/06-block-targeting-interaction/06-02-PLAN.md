---
phase: 06-block-targeting-interaction
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - voxx-js/main.js
  - voxx-js/chunk.js
autonomous: true
requirements:
  - EDIT-01
must_haves:
  truths:
    - "Left-click removes block under crosshair"
    - "Removed block shows as air (empty space)"
    - "Chunk mesh updates immediately after block removal"
  artifacts:
    - path: voxx-js/main.js
      provides: "destroyBlock() function and mousedown event handler"
      contains: "chunk.setVoxel(hit.localX, hit.localY, hit.localZ, 0)"
    - path: voxx-js/chunk.js
      provides: "setVoxel() method to modify block data"
      exports: ["setVoxel", "updateMesh"]
  key_links:
    - from: voxx-js/main.js
      to: voxx-js/chunk.js
      via: "chunk.setVoxel() writes to voxel array, chunk.updateMesh() rebuilds"
      pattern: "setVoxel.*0\|updateMesh\(true\)"
    - from: voxx-js/main.js
      to: voxx-js/world.js
      via: "world.chunks[key] access for chunk lookup"
      pattern: "world.chunks\["
---

<objective>
Implement left-click block destruction. When player left-clicks while looking at a block, the block is removed and the chunk mesh updates.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

Existing codebase:
- voxx-js/main.js:201-202 has mousedown handler calling destroyBlock()
- voxx-js/main.js:531-543 has destroyBlock() implementation
- voxx-js/chunk.js:48-55 has setVoxel() method
- voxx-js/chunk.js:298-321 has updateMesh() method with forceUpdate parameter
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement destroyBlock() function</name>
  <files>voxx-js/main.js</files>
  <action>
    Implement destroyBlock() that:
    1. Calls raycastBlock() to get targeted block
    2. Returns early if hit is false
    3. Gets chunk from world.chunks[`${hit.chunkX},${hit.chunkZ}`]
    4. Returns early if chunk not found
    5. Calls chunk.setVoxel(hit.localX, hit.localY, hit.localZ, 0) to set to air
    6. Calls chunk.updateMesh(true) with forceUpdate=true for immediate visual feedback
    
    The setVoxel() method in chunk.js handles bounds checking internally.
    Setting value to 0 = BLOCK_TYPES.AIR which removes the block.
  </action>
  <verify>
    <automated>grep -A20 "function destroyBlock" voxx-js/main.js</automated>
    <manual>Left-click on a block - it should disappear immediately</manual>
  </verify>
  <done>destroyBlock() removes block at targeted position and updates chunk mesh</done>
</task>

<task type="auto">
  <name>Task 2: Wire mousedown event to destroyBlock</name>
  <files>voxx-js/main.js</files>
  <action>
    Verify mousedown event handler:
    1. Only processes when isPointerLocked is true
    2. Calls event.preventDefault() to prevent text selection
    3. Checks event.button === 0 for left click
    4. Calls destroyBlock() for left click
    
    Current code should be:
    document.addEventListener('mousedown', (event) => {
      if (!isPointerLocked) return;
      event.preventDefault();
      if (event.button === 0) {
        destroyBlock();
      } else if (event.button === 2) {
        placeBlock();
      }
    });
  </action>
  <verify>
    <automated>grep -B2 -A8 "event.button === 0" voxx-js/main.js</automated>
    <manual>Left-click block - should trigger destroyBlock()</manual>
  </verify>
  <done>Left mouse button triggers block destruction when pointer locked</done>
</task>

<task type="auto">
  <name>Task 3: Verify chunk mesh update after block removal</name>
  <files>voxx-js/chunk.js</files>
  <action>
    Verify updateMesh(forceUpdate=true) behavior:
    1. When forceUpdate=true, calls _updateMeshInPlace() for immediate update
    2. _updateMeshInPlace() generates new mesh data via generateMeshData()
    3. Replaces geometry in-place to avoid mesh flash
    4. Marks needsUpdate = false after completion
    
    generateMeshData() uses greedy meshing algorithm that:
    - Iterates through all voxel positions
    - Creates faces only for exposed surfaces
    - Excludes faces between two solid blocks (hidden faces)
    
    After removal, the hole shows interior faces of surrounding blocks.
  </action>
  <verify>
    <automated>grep -n "updateMesh\|forceUpdate" voxx-js/chunk.js | head -10</automated>
    <manual>Break block - surrounding blocks should show new interior faces</manual>
  </verify>
  <done>Chunk mesh rebuilds immediately, showing hole where block was removed</done>
</task>

</tasks>

<verification>
Run the application:
1. Left-click on various block types (stone, dirt, grass)
2. Verify block disappears immediately
3. Verify surrounding blocks show interior faces
4. Break through terrain to create holes - mesh should update correctly
5. Check FPS remains stable during block breaking
</verification>

<success_criteria>
- [ ] Left-click triggers block removal
- [ ] Targeted block set to air (value 0)
- [ ] Chunk mesh updates immediately without flash
- [ ] Interior faces visible after block removal
- [ ] No errors in console during block breaking
</success_criteria>

<output>
After completion, create `.planning/phases/06-block-targeting-interaction/06-02-SUMMARY.md`
</output>
