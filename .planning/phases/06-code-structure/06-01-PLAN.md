---
phase: 06-code-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - voxx-js/src/constants.js
  - voxx-js/chunk.js
  - voxx-js/chunkCore.js (delete)
  - voxx-js/src/main.js (update import)
  - voxx-js/src/math/utils.js
  - voxx-js/src/math/index.js
autonomous: true
requirements:
  - STRUCT-01
  - STRUCT-04

must_haves:
  truths:
    - "Chunk constants are in a single location (src/constants.js)"
    - "Duplicate chunkCore.js is removed"
    - "Math utilities exist at src/math/utils.js with matrix operations"
    - "All imports work correctly"
  artifacts:
    - path: "voxx-js/src/constants.js"
      provides: "CHUNK_WIDTH, CHUNK_HEIGHT, CHUNK_DEPTH constants"
      exports: ["CHUNK_WIDTH", "CHUNK_HEIGHT", "CHUNK_DEPTH"]
    - path: "voxx-js/src/math/utils.js"
      provides: "Matrix and vector operations (multiplyMatrices, lookAt, normalize, cross, dot)"
      exports: ["multiplyMatrices", "lookAt", "normalize", "cross", "dot", "createViewMatrix", "createProjectionMatrix"]
    - path: "voxx-js/src/math/index.js"
      provides: "Re-exports from utils"
      exports: ["multiplyMatrices", "lookAt", "normalize", "cross", "dot"]
  key_links:
    - from: "voxx-js/chunk.js"
      to: "src/constants.js"
      via: "import"
      pattern: "from '.*constants'"
    - from: "voxx-js/src/main.js"
      to: "src/math/utils.js"
      via: "import"
      pattern: "from '.*math"
---

<objective>
Create foundation modules: chunk constants and math utilities. This is the base layer that other modules depend on.
</objective>

<execution_context>
@/home/box/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/box/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-code-structure/06-CONTEXT.md

# Reference files for understanding current structure
@voxx-js/chunk.js
@voxx-js/chunkCore.js
@voxx-js/src/main.js (lines 188-200 for matrix functions, lines 444-510 for camera math)
</context>

<tasks>

<task type="auto">
  <name>Create constants.js with chunk dimensions</name>
  <files>voxx-js/src/constants.js</files>
  <action>
Create src/constants.js with the following exports from chunkCore.js:
- CHUNK_WIDTH = 32
- CHUNK_HEIGHT = 256
- CHUNK_DEPTH = 32

This consolidates constants that are currently duplicated across chunk.js and chunkCore.js.
  </action>
  <verify>File exists with correct exports. Run: node -e "import('./voxx-js/src/constants.js').then(m => console.log(m.CHUNK_WIDTH, m.CHUNK_HEIGHT, m.CHUNK_DEPTH))"</verify>
  <done>src/constants.js exports CHUNK_WIDTH, CHUNK_HEIGHT, CHUNK_DEPTH</done>
</task>

<task type="auto">
  <name>Consolidate chunk.js and remove chunkCore.js</name>
  <files>voxx-js/chunk.js, voxx-js/chunkCore.js</files>
  <action>
1. Update chunk.js to import constants from src/constants.js instead of chunkCore.js
2. Remove the duplicate chunkCore.js file
3. Update any other files that import from chunkCore.js to use src/constants.js

Current imports in chunk.js (line 8):
  import { CHUNK_WIDTH, CHUNK_HEIGHT, CHUNK_DEPTH } from './chunkCore.js';
Change to:
  import { CHUNK_WIDTH, CHUNK_HEIGHT, CHUNK_DEPTH } from './src/constants.js';

Also check other files that may import from chunkCore.js and update them.
  </action>
  <verify>chunkCore.js is deleted, chunk.js imports from src/constants.js. Manual game test to verify world loads.</verify>
  <done>Single Chunk class exists in chunk.js without duplicate chunkCore.js</done>
</task>

<task type="auto">
  <name>Create math utilities module</name>
  <files>voxx-js/src/math/utils.js, voxx-js/src/math/index.js</files>
  <action>
Create src/math/utils.js with the following functions extracted from main.js:
- multiplyMatrices(a, b) - 4x4 matrix multiplication (lines 188-199 in main.js)
- lookAt(eye, center, up) - view matrix lookAt function (lines 466-497)
- normalize(v) - vector normalization (lines 484-487)
- cross(a, b) - cross product (lines 489-495)
- dot(a, b) - dot product (lines 497-500)
- createViewMatrix() - wrapper using camera state (line 444-464)
- createProjectionMatrix() - perspective projection (lines 581-597)

Also create src/math/index.js that re-exports all functions from utils.js.
  </action>
  <verify>Module imports correctly. Run: node -e "import('./voxx-js/src/math/index.js').then(m => console.log(typeof m.multiplyMatrices))"</verify>
  <done>Math utilities module exists at src/math/utils.js with matrix operations</done>
</task>

<task type="auto">
  <name>Update main.js imports to use new modules</name>
  <files>voxx-js/src/main.js</files>
  <action>
Update main.js to import constants from src/constants.js instead of chunkCore.js.
Also import math functions from src/math/ where applicable.

Note: This plan extracts the foundation. We'll update main.js to use math utils in plan 06-02.
  </action>
  <verify>main.js loads without errors. Manual game test.</verify>
  <done>main.js uses new constants import</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Verify chunkCore.js is deleted
2. Verify src/constants.js exists with chunk dimensions
3. Verify src/math/utils.js exists with matrix functions
4. Manual game test: run index.html in browser, verify world loads and renders correctly
</verification>

<success_criteria>
STRUCT-01: Single Chunk class exists (src/chunk.js) without duplicate chunkCore.js
STRUCT-04: Math utilities module exists at src/math/utils.js with matrix operations
</success_criteria>

<output>
After completion, create `.planning/phases/06-code-structure/06-01-SUMMARY.md`
</output>