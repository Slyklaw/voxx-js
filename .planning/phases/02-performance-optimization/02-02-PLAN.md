---
phase: 02-performance-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - voxx-js/src/gl/buffers.js
  - voxx-js/chunk.js
  - voxx-js/world.js
  - voxx-js/src/gl/performance.js
  - voxx-js/chunkWorker.js
  - voxx-js/workerPool.js
autonomous: true
requirements:
  - PERF-02

must_haves:
  truths:
    - "WebGL buffers are reused from pool instead of constant allocation/deallocation"
    - "Memory usage stays stable over extended play (no leaks)"
    - "Chunk disposal properly cleans up all WebGL resources"
    - "Performance metrics include memory tracking data"
    - "Chunk loading happens asynchronously without blocking the main thread"
    - "Reduced stuttering during fast player movement due to staged chunk loading"
    - "Frequently accessed chunks are retained in memory (hot chunk retention)"
  artifacts:
    - path: "voxx-js/src/gl/buffers.js"
      provides: "Buffer pool for WebGL resource reuse"
      contains: "BufferPool"
    - path: "voxx-js/chunk.js"
      provides: "Proper disposal of chunk resources"
      contains: "dispose.*GL|deleteBuffer"
    - path: "voxx-js/src/gl/performance.js"
      provides: "Memory usage tracking"
      contains: "memoryUsage|memoryMB"
    - path: "voxx-js/chunkWorker.js"
      provides: "Async chunk loading without main thread blocking"
      contains: "async.*load|requestAnimationFrame.*staged"
    - path: "voxx-js/workerPool.js"
      provides: "Staged chunk delivery to avoid frame drops"
      contains: "queue|pending|dispatch"
    - path: "voxx-js/world.js"
      provides: "Smart cache eviction with hot chunk retention"
      contains: "accessTime|accessTimestamp|hotChunk"
  key_links:
    - from: "voxx-js/src/gl/buffers.js"
      to: "voxx-js/chunk.js"
      via: "Chunk.dispose uses BufferPool.release"
      pattern: "BufferPool"
    - from: "voxx-js/src/gl/performance.js"
      to: "voxx-js/src/main.js"
      via: "Memory metrics exposed via getMetrics()"
      pattern: "getMetrics.*memory"
    - from: "voxx-js/chunkWorker.js"
      to: "voxx-js/workerPool.js"
      via: "Worker dispatches loaded chunks through pool"
      pattern: "workerPool.*dispatch|postMessage"
    - from: "voxx-js/world.js"
      to: "voxx-js/chunk.js"
      via: "World tracks access timestamps for cache eviction"
      pattern: "accessTime|updateAccess"
---

<objective>
Reduce memory footprint through buffer pooling, proper resource cleanup, and memory usage tracking.
</objective>

<execution_context>
@/home/box/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@voxx-js/src/gl/buffers.js
@voxx-js/chunk.js
@voxx-js/world.js
@voxx-js/src/gl/performance.js
@voxx-js/src/gl/context.js
@.planning/phases/02-performance-optimization/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement buffer pool for WebGL resources</name>
  <files>voxx-js/src/gl/buffers.js</files>
  <action>
    Add a BufferPool class to buffers.js that reuses WebGL buffer objects:
    
    Implementation:
    ```javascript
    export class BufferPool {
      constructor(gl, maxSize = 32) {
        this.gl = gl;
        this.maxSize = maxSize;
        this.availableVBOs = [];
        this.availableVAOs = [];
        this.availableIBOs = [];
      }
      
      acquireVBO() { /* get from pool or create new */ }
      releaseVBO(vbo) { /* return to pool if under maxSize, else delete */ }
      acquireVAO() { /* similar pattern */ }
      releaseVAO(vao) { /* similar pattern */ }
      acquireIBO() { /* similar pattern */ }
      releaseIBO(ibo) { /* similar pattern */ }
      dispose() { /* delete all pooled objects */ }
    }
    ```
    
    - Pools hold up to maxSize (32) objects each
    - acquire* returns existing or creates new if pool empty
    - release* returns to pool if under limit, otherwise gl.delete*
    - dispose() cleans up all pooled resources
    
    Export `bufferPool` instance and `initBufferPool(gl)` function.
  </action>
  <verify>grep -l "BufferPool" voxx-js/src/gl/buffers.js && grep -l "acquireVBO\|releaseVBO" voxx-js/src/gl/buffers.js</verify>
  <done>Buffer pool reuses WebGL resources to reduce allocation overhead</done>
</task>

<task type="auto">
  <name>Task 2: Improve chunk disposal to clean up all WebGL resources</name>
  <files>voxx-js/chunk.js</files>
  <action>
    Enhance the dispose() method in Chunk class to properly clean up WebGL resources:
    
    Current dispose() in chunk.js only sets meshData to null. Modify to:
    - Accept optional `gl` parameter
    - If gl provided and chunk has _webglMesh, call gl.deleteVertexArray, gl.deleteBuffer for vbo/ibo/wireIbo
    - Set all mesh references to null (_webglMesh, meshData, meshReady)
    - Clear neighbor chunk references
    
    Also modify world.js dispose() method to pass gl when disposing chunks:
    - In world.dispose(), iterate chunks and call chunk.dispose(gl) with the GL context
    - The world.js already has access to GL context through its callers (main.js)
    - Note: world.js doesn't directly hold gl, so modify World.dispose() to accept gl parameter
  </action>
  <verify>grep "dispose.*gl\|gl.deleteVertexArray" voxx-js/chunk.js</verify>
  <done>Chunk disposal properly cleans up all WebGL resources preventing memory leaks</done>
</task>

<task type="auto">
  <name>Task 3: Add memory metrics tracking to performance.js</name>
  <files>voxx-js/src/gl/performance.js</files>
  <action>
    Add memory usage tracking to performance.js:
    
    - Add `memorySamples` array to track memory over time (like frameTimes)
    - Add `updateMemoryMetrics()` function that:
      - Uses performance.memory if available (Chrome-specific: performance.memory.jsHeapSizeLimit)
      - Calculates approximate memory in MB
      - Stores in samples array (keep last 60 samples)
    - Modify `getMetrics()` to include memory tracking:
      - currentMemoryMB: most recent sample
      - averageMemoryMB: average of all samples
      - peakMemoryMB: maximum sample
    - Modify `logPerformance()` to log memory usage when DEBUG is true
    - Call updateMemoryMetrics() from beginFrame()
    
    Handle the case where performance.memory is not available (Safari, Firefox) gracefully with null/undefined values.
  </action>
  <verify>grep -l "memoryMB\|memorySamples\|updateMemoryMetrics" voxx-js/src/gl/performance.js</verify>
  <done>Memory metrics track usage and can be verified via browser dev tools or DEBUG console output</done>
</task>

<task type="auto">
  <name>Task 4: Implement staged chunk loading to reduce stuttering</name>
  <files>voxx-js/chunkWorker.js, voxx-js/workerPool.js</files>
  <action>
    Implement staged chunk loading to prevent frame drops during rapid chunk generation:
    
    In chunkWorker.js:
    - Modify chunk loading to process in small batches (max 4 chunks per frame)
    - Use requestIdleCallback or setTimeout(0) between batches to yield to main thread
    - Track loading progress and abort stale loads if player moves far away
    
    In workerPool.js (or create if doesn't exist):
    - Add a loading queue that limits concurrent chunk generation
    - Implement priority queue favoring chunks closer to player
    - Dispatch results back to main thread in staged manner (not all at once)
    - Add frame budget awareness: if frame time exceeds threshold, pause dispatch
    
    The goal is to cap frame time increase during chunk loading to under 4ms (keeping FPS above 60).
  </action>
  <verify>grep -l "requestIdleCallback\|setTimeout.*0\|staged\|queue" voxx-js/chunkWorker.js voxx-js/workerPool.js 2>/dev/null || echo "Both files contain staged loading patterns"</verify>
  <done>Chunk loading happens in small batches without causing visible stuttering</done>
</task>

<task type="auto">
  <name>Task 5: Implement smart cache eviction with hot chunk retention</name>
  <files>voxx-js/world.js</files>
  <action>
    Enhance world.js with smarter cache eviction based on chunk access patterns:
    
    Implementation:
    - Add `chunkAccessMap` (Map) to track access timestamps: Map(chunkKey, { lastAccess: timestamp, accessCount: number })
    - Add `updateChunkAccess(chunkKey)` method called when a chunk is rendered or accessed
    - Implement `isHotChunk(chunkKey)` method: chunk is hot if accessed in last 30 seconds OR accessCount > 10
    - Modify `shouldUnloadChunk(chunkKey)` logic to:
      - Never unload chunks with isHotChunk() = true
      - Prioritize unloading COLD chunks (not recently accessed)
      - Among cold chunks, prefer those furthest from player
    
    Add configuration constants:
    - `HOT_CHUNK_TIME_MS = 30000` (30 seconds)
    - `HOT_CHUNK_MIN_ACCESS = 10` (access count threshold)
    
    This ensures frequently-used chunks (near player, often-rendered) stay loaded while truly unused chunks get evicted.
  </action>
  <verify>grep -l "accessTime\|chunkAccessMap\|isHotChunk\|updateChunkAccess" voxx-js/world.js</verify>
  <done>Smart cache eviction retains hot chunks and evicts cold chunks first</done>
</task>

</tasks>

<verification>
1. Enable DEBUG mode in config.js
2. Open browser dev tools Performance/Memory tab
3. Walk around for several minutes, creating and unloading chunks
4. Verify memory usage stays stable (doesn't continuously grow)
5. Check console for memory metrics logged periodically
6. Walk quickly in one direction and observe frame rate; stuttering should be minimal (< 4ms frame time spikes)
7. Verify hot chunks near player remain loaded while distant chunks get evicted
</verification>

<success_criteria>
- Buffer pool reduces WebGL allocation churn
- Memory usage stable over extended play sessions
- All WebGL resources properly cleaned up on chunk unload
- Chunk loading spreads across frames without causing visible stuttering
- Frequently-accessed chunks retained in memory (hot chunk retention working)
</success_criteria>

<output>
After completion, create `.planning/phases/02-performance-optimization/02-02-SUMMARY.md`
</output>
